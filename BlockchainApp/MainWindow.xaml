<Window x:Class="BlockchainApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BlockchainApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="328.2">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="6"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <ListView Grid.Column="1" >
            
        </ListView>
        <GridSplitter Grid.Column="1"  Width="5"/>
        <Grid Grid.Column="2" Width="{Binding AddPanelWidth}">
            <Button Content="Open" HorizontalAlignment="Left" Margin="-50,10,0,0" VerticalAlignment="Top" Width="50"/>
            <Button Content="Add" HorizontalAlignment="Left" Margin="-50,30,0,0" VerticalAlignment="Top" Width="50"/>
        </Grid>

    </Grid>
</Window>
